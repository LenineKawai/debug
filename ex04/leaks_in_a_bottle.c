#include <stdio.h>
#include <unistd.h>
#include <stdlib.h>
#include <string.h>

int main (int ac, char **av)
{
    int i = 1;
    char **str_array = malloc(ac * sizeof(str_array));
    while (i < ac)
    {
        str_array[i] = substr
    }
}